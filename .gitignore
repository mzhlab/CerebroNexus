.DS_Store
CerebroNexus.Rcheck
CerebroNexus_*.tar.gz
.direnv
.Rhistory
.Rprofile
result
vignettes/*.crb
docs
!docs/
docs/*
!docs/builder-projects.md
pkgdown-site/
.Rproj.user
*.Rproj
tests/smoke/
.playwright-mcp/
tmp/

# local per-project memory (Loci brain) — personal, never tracked
.loci/

# shinytest2 snapshot comparison artifacts
tests/testthat/_snaps/**/*.new.png
tests/testthat/_artifacts/

# large public raw data — auto-downloaded by the build scripts, reproducible via
# data-raw/DATASETS.md, not tracked (only the built .crb ship)
data-raw/vdj_10x/
data-raw/vdj_10x_dextramer/
data-raw/slidetags/
data-raw/xenium/

# R's default graphics-device dump, produced by tests/vignettes that plot; a
# stray artifact, never source
Rplots.pdf
**/Rplots.pdf
# The bare `result` rule near the top of this file matches a directory of that
# name at any depth, which also swallowed the benchmark results. Those are the
# deliverable, not scratch output, so re-include them -- the directory has to be
# re-included first, because a file cannot be un-ignored while a parent
# directory of it is still excluded.
!tests/bench/result/
!tests/bench/result/*.csv
!tests/bench/result/summary.md
tests/bench/result/logs/
!tests/bench/result/CURRENT
!tests/bench/result/runs/
!tests/bench/result/runs/**/
!tests/bench/result/runs/**/*.csv
!tests/bench/result/runs/**/summary.md
tests/bench/result/runs/**/logs/
!tests/bench/result/archive/
!tests/bench/result/archive/**/
!tests/bench/result/archive/**/*.csv
!tests/bench/result/archive/**/summary.md
!tests/bench/result/archive/**/README.md
tests/bench/scratch/

# builder QA fixtures — regenerated by data-raw/build_builder_fixtures.R
data-raw/builder_fixtures/

# Runtime database for revocable Linked-view share links.
inst/private-data/linked-view-shares.sqlite*
