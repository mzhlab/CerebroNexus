# Share selection label

Change the Linked views configuration launcher label from `Share views` to the
fixed text `Share selection`.

The label does not rotate or react to selection state. The dialog contents,
availability rules, accessible title, JSON import/export, Saved views, and share
link behavior remain unchanged. Update source and browser-facing label contracts.
