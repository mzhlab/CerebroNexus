# Consensus string of equal-length sequences (differing positions -\> "x")

Consensus string of equal-length sequences (differing positions -\> "x")

## Usage

``` r
hla_make_consensus(seqs)
```

## Arguments

- seqs:

  Character vector of equal-length sequences.

## Value

One consensus string; positions that differ across members are "x".
