# Largest node radius, in px. One enormous clone must not swallow the layout.

Largest node radius, in px. One enormous clone must not swallow the
layout.

## Usage

``` r
HLA_NODE_R_MAX
```

## Format

An object of class `numeric` of length 1.
