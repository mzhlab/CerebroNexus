# Clone size above which node area stops being proportional (the cap bites).

Clone size above which node area stops being proportional (the cap
bites).

## Usage

``` r
HLA_NODE_MAX_EXACT
```

## Format

An object of class `numeric` of length 1.
